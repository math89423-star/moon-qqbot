# 粟藜 · LLM 配置桥接

增强插件。从外部数据库读取 LLM 配置，提供 `/chat` 对话命令。

## 命令

| 命令 | 说明 |
|------|------|
| `/chat <消息>` | 与 AI 对话 |
| `/chat reset` | 重置会话 |
| `/chat model` | 查看当前模型 |
| `/chat list` | 列出可用模型 |
| `/chat switch <id>` | 切换模型 |

## 特性

- 多轮对话，30 分钟无操作自动清理
- 只读桥接，不修改外部数据库

